server:
  port: 8082

spring:
  application:
    name: fraud
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  datasource:
    url: jdbc:postgresql://localhost:5444/fraud
    username: amigoscode
    password: password
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka